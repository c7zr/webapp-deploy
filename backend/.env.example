# Instagram Report Bot - Environment Configuration
# Copy this file to .env and configure for your environment

# Security Settings
SECRET_KEY=your-super-secret-jwt-key-change-this-in-production
ALGORITHM=HS256

# Database Configuration (Production)
# For production: Use external database server, NOT localhost
# DATABASE_TYPE=postgresql  # or mysql
# DATABASE_HOST=your-db-server.example.com
# DATABASE_PORT=5432
# DATABASE_NAME=instagram_report_bot
# DATABASE_USER=db_user
# DATABASE_PASSWORD=strong_password_here

# For development only (not recommended for production)
DATABASE_TYPE=sqlite
DATABASE_PATH=./swatnfo.db

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Rate Limiting
RATE_LIMIT_PER_MINUTE=60
MAX_FAILED_LOGIN_ATTEMPTS=5
ACCOUNT_LOCKOUT_MINUTES=15

# Session Configuration
SESSION_TIMEOUT_HOURS=24
JWT_EXPIRATION_HOURS=24

# Instagram API (if using)
# INSTAGRAM_SESSION_ID=
# INSTAGRAM_CSRF_TOKEN=

# Security Recommendations:
# 1. NEVER commit .env file to git (already in .gitignore)
# 2. Use strong, unique SECRET_KEY (generate with: openssl rand -hex 32)
# 3. Database MUST be on separate server in production
# 4. Use HTTPS only in production
# 5. Keep all dependencies updated
# 6. Regular security audits with: pip-audit or safety check
